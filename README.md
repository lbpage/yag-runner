# Yet Another Github Runner
I created this project as a way to build and deploy github runner service with nix. The resulting binaries are built using nix and are available as nix flakes.

## Installation

## Usage